package org.digimad.migration.constants;

public class QueryConstants {
    //    public static final String INSERT = "INSERT INTO #toTable# SELECT #fromColumnStr# FROM #fromTable# WHERE #condition#";
    public static final String INSERT = "INSERT INTO %s SELECT %s FROM %s WHERE %s";
}
